spring.application.name=react-app-2

spring.datasource.url=jdbc:postgresql://localhost:5433/nosaintz
spring.datasource.username=postgres
spring.datasource.password=N1i9k9e9#!database
spring.datasource.driver-class-name=org.postgresql.Driver

# Run backend on port 5713 for local dev
server.port=5713

# Enable Hibernate to create/update schema automatically for local dev
spring.jpa.hibernate.ddl-auto=create

# Disable automatic data.sql since seeding is handled by DataLoader
spring.sql.init.mode=never

# Defer running data scripts until after JPA has created the schema (kept for safety)
spring.jpa.defer-datasource-initialization=true

# Allowed CORS origins for dev (comma-separated)
app.cors.allowed-origins=http://localhost:5173,http://localhost:5174

# Google Calendar API
google.api.key=AIzaSyDGNhhC3Med0jmuL9l_aooyIuBy0kbnZ94
google.calendar.id=info.nosaintz@gmail.com

# Email Configuration (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=info.nosaintz@gmail.com
spring.mail.password=${GMAIL_APP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000